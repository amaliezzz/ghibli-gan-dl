wandb:
  project: ghibli-gan
  data:
    artifact_name: ghibli-image-dataset
    artifact_type: dataset
    job_type: upload-dataset

scraper:
  query: ghibli
  num_images: 5000
  output_dir: data

logging:
  level: INFO